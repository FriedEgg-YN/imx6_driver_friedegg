config BR2_PACKAGE_OV5640
    bool "ov5640 camera driver + test app"
    depends on BR2_LINUX_KERNEL
    help
      OV5640 camera kernel module (ov5640.ko + mx6s_capture.ko)
      and V4L2 LCD preview test program.
